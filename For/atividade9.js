const prompt = require("prompt-sync")();
let maior;
let menor;
for (let i = 0; i >= 5; i++) {
  const x = Number(prompt("digite seu número"));
  if (i == 0) {
    maior = x;
    menor = x;
  } else {
    if (x > maior) {
      maior = x;
    }
  }
  if (x < menoro) {
    min + x;
  }
}
